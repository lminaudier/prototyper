# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'haml', :input => 'site', :output => 'output' do
  watch(/^.+(\.html\.haml)/)
end

guard 'sass', :input => 'site/sass', :output => 'output/styles'
